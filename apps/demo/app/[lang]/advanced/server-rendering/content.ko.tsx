import Link from 'next/link'

export default function SsrKo() {
  return (
    <div className="doc" lang="ko">
      <h1>서버 렌더링</h1>
      <p className="lede">
        서버는 사용자의 화면 크기도, 터치인지 마우스인지도 몰라요. 그래서 환경에 따라
        바뀌는 UI는 서버 렌더링과 만나면 화면이 깜빡이거나(잘못된 모습이 먼저 보였다가
        바뀜) hydration 오류가 나기 쉬워요. Protean은 &quot;더 잘 추측하기&quot; 대신,
        추측이 필요 없게 문제를 재구성했어요.
      </p>

      <h2>원칙 하나</h2>
      <div className="callout">
        서버가 틀릴 수 있는 판단은 CSS로 표현할 수 있어야 해요. 그럴 수 없다면, 사용자가
        상호작용하는 순간까지 미뤄야 해요. 둘 다 안 되는 컴포넌트는 만들지 않아요.
      </div>

      <h2>다이얼로그: 열리는 순간에 정해요</h2>
      <p>
        닫혀 있는 다이얼로그는 아무것도 그리지 않아요. 모달로 열지 시트로 열지는
        사용자가 버튼을 누르는 순간, 브라우저에서 정해요. 서버가 보낸 HTML에는 버튼만
        있고 다이얼로그 마크업이 아예 없으니, 깜빡일 것도 어긋날 것도 없어요. 직접
        확인해 보세요.
      </p>
      <pre><code>{`curl -s <이 사이트 주소>/ssr-proof | grep -c 'data-part="popup"'
# 결과: 0  (서버 HTML에 다이얼로그가 0개)`}</code></pre>

      <h2>내비게이션: DOM은 하나, 모습은 CSS가 바꿔요</h2>
      <p>
        내비게이션은 첫 화면부터 보여야 하니 미룰 수 없어요. 그래서 반대쪽 방법을 써요.{' '}
        <code>nav &gt; ul</code> 트리 하나를 만들고, 하단 탭 바 - 서랍 - 레일 -
        사이드바를 그 트리의 CSS 상태 네 개로 만들었어요. 서버는 모든 사용자에게 같은
        HTML을 보내고, 첫 모습은 브라우저의 미디어 쿼리가 정해요. 마크업이 같으니
        첫 배치는 구조적으로 틀릴 수가 없어요. 실측 결과 화면 밀림(CLS)은 0이고,
        JavaScript가 아직 안 왔어도 첫 배치가 올바르게 나와요 - 서랍을 여닫는 것
        같은 상호작용은 JavaScript가 필요해요.{' '}
        <Link href="/navigation-spike">내비게이션 데모</Link>에서 확인할 수 있어요.
      </p>

      <h2>JavaScript로 환경을 읽는 부분은요?</h2>
      <p>
        이 문서 사이트처럼 &quot;작은 화면에서는 사이드바를 아예 안 그린다&quot; 같은
        판단을 JavaScript로 하는 경우도 있어요. 이때는 React가 공식으로 안내하는 방식을
        그대로 따라요. hydration 중에는 서버가 가정한 값을 그대로 써서 오류를 피하고,
        그 직후 실제 측정값으로 한 번 고쳐요. 서버 가정값의 기본은 &quot;작은 화면 +
        터치&quot;이고, 원하면 쿠키 등으로 바꿔 줄 수 있어요.
      </p>

      <h2>왜 User-Agent로 기기를 알아내지 않나요?</h2>
      <ul>
        <li>더 정밀한 힌트(Client Hints)는 Chrome 계열 전용이에요. Safari와 Firefox는 공식적으로 채택하지 않겠다고 밝혔어요.</li>
        <li>User-Agent 문자열은 태블릿을 자주 틀려요.</li>
        <li>기기마다 다른 HTML을 보내면 CDN 캐시가 기기 수만큼 쪼개져요.</li>
      </ul>
      <p>
        위의 원칙을 지키면 기본 규칙에서는 서버가 추측할 일 자체가 없어요. 경쟁이 아니라
        회피가 답이었어요.
      </p>
    </div>
  )
}
